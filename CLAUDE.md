# CLAUDE.md

`AGENTS.md` is the single source of repository instructions. Read and follow it completely before changing this project.

The backend is the final Rust implementation. Do not restore the removed Elysia/Drizzle/TypeScript backend or add compatibility paths; verify the boundary with `bun run cutover:check`.
